"use client";
import type { Lang } from "@/content/i18n";
/* Placeholder: the load-in curtain is built in a separate pass. */
export default function Preloader({ lang }: { lang: Lang }) { void lang; return null; }
